import { GoToLinkDirective } from './go-to-link.directive';

describe('GoToLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new GoToLinkDirective();
    expect(directive).toBeTruthy();
  });
});
