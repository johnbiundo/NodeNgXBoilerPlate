import { EmailAvailable } from './email-available.directive';

describe('Emailavailable.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailAvailable();
    expect(directive).toBeTruthy();
  });
});
