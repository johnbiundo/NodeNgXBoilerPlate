import { CloudMediaValidatorDirective } from './cloud-media-validator.directive';

describe('CloudMediaValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CloudMediaValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
